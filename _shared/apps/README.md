# WAIBA App Shared Libraries

Shared libraries in this folder are for native app UI learning and app navigation.

They are separate from web navigation libraries. Use them for Windows applications such as IDEs, editors, design tools, terminals, and dynamic workspace apps.
